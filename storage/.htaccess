# storage/ sits outside the web root. This file is a safety net in case the
# DocumentRoot is ever misconfigured to the project root.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
